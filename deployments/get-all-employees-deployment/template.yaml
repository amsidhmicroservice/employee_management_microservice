AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: Get All Employees Lambda Function

Globals:
  Api:
    # Global API Gateway configurations
    EndpointConfiguration: REGIONAL
    Cors:
      AllowMethods: "'GET'"
      AllowHeaders: "'Content-Type,X-Amz-Date,Authorization,X-Api-Key'"
      AllowOrigin: "'*'"
  Function:
    Timeout: 10
    Runtime: python3.13
    Architectures:
      - x86_64
    Tracing: Active

Resources:
  SharedLayerResource:
    Type: AWS::Serverless::LayerVersion
    Properties:
      LayerName: shared-layer
      ContentUri: ../../commons/layers/shared
      CompatibleRuntimes:
        - python3.13

  # Define the API Gateway
  EmployeeApiGatewayResource:
    Type: AWS::Serverless::Api
    Properties:
      Name: get-all-employees-api-gateway
      StageName: Prod
      EndpointConfiguration:
        Type: REGIONAL
      Auth:
        ApiKeyRequired: true

  GetAllEmployeesLambdaFunctionResource:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: GetAllEmployeesFunction
      CodeUri: ../../get_all_employees_service/get_all_employees_lambda/src/
      Handler: handler.get_all_employees_handler.lambda_handler
      Layers:
        - !Ref SharedLayerResource
      Environment:
        Variables:
          TABLE_NAME: !ImportValue shared-resources-stack-EmployeeTableName
          LOG_LEVEL: INFO
      Events:
        GetAllEmployeesAPI:
          Type: Api
          Properties:
            RestApiId: !Ref EmployeeApiGatewayResource
            Path: /employees
            Method: get
      Policies:
        - AWSLambdaBasicExecutionRole
        - Statement:
            - Effect: Allow
              Action:
                - dynamodb:Scan
              Resource: !ImportValue shared-resources-stack-EmployeeTableArn

  GetAllEmployeesAPIGatewayPermission:
    Type: AWS::Lambda::Permission
    Properties:
      Action: lambda:InvokeFunction
      FunctionName: !GetAtt GetAllEmployeesLambdaFunctionResource.Arn
      Principal: apigateway.amazonaws.com
      SourceArn: !Sub "arn:aws:execute-api:${AWS::Region}:${AWS::AccountId}:${EmployeeApiGatewayResource}/*/GET/employees"

  GetAllEmployeeApiKey:
    Type: AWS::ApiGateway::ApiKey
    DependsOn:
      - EmployeeApiGatewayResource
    Properties:
      Name: GetAllEmployeeApiKey
      Description: API Key for Get All Employee API
      Enabled: true

  EmployeeApiUsagePlan:
    Type: AWS::ApiGateway::UsagePlan
    DependsOn:
      - EmployeeApiGatewayResource
      - GetAllEmployeesLambdaFunctionResource
    Properties:
      UsagePlanName: EmployeeApiUsagePlan
      Description: Usage plan for Employee API
      ApiStages:
        - ApiId: !Ref EmployeeApiGatewayResource
          Stage: Prod
      Quota:
        Limit: 1000
        Period: MONTH
      Throttle:
        BurstLimit: 10
        RateLimit: 5

  EmployeeApiUsagePlanKey:
    Type: AWS::ApiGateway::UsagePlanKey
    DependsOn: EmployeeApiUsagePlan
    Properties:
      KeyId: !Ref GetAllEmployeeApiKey
      KeyType: API_KEY
      UsagePlanId: !Ref EmployeeApiUsagePlan

Outputs:
  GetAllEmployeesApiUrl:
    Description: API Gateway endpoint URL for Get All Employees
    Value: !Sub "https://${EmployeeApiGatewayResource}.execute-api.${AWS::Region}.amazonaws.com/Prod/employees"

  ApiGatewayId:
    Description: API Gateway ID
    Value: !Ref EmployeeApiGatewayResource

  ApiKey:
    Description: API Key ID
    Value: !Ref GetAllEmployeeApiKey