[pytest]
# Add the shared layer to PYTHONPATH
pythonpath =
    ../commons/layers/shared/python

# Tell pytest to look only in the shared tests folder
testpaths =
    shared

# Coverage options (optional)
addopts =
    --cov=shared
    --cov-report=term-missing
    --cov-report=html:tests/coverage/shared/report
